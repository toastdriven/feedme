<h1>Editing rss_feed</h1>

<%= error_messages_for :rss_feed %>

<% form_for(@rss_feed) do |f| %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @rss_feed %> |
<%= link_to 'Back', rss_feeds_path %>
